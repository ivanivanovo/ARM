\ связанные списки
: NewChain ( <name> --) \ создать новую цепочку с именем <name>
    CREATE 0 , 0 ,
    ;
: inject ( adr1 u1 chain --) \ вставить объект с именем adr1 u1 в цепь chain
    . TYPE CR
    ;